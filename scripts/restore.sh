#!/usr/bin/env bash
# เลิกใช้แล้ว — ระบบกู้คืนข้อมูลย้ายเข้าไปอยู่ในแอปแล้ว
# ใช้ปุ่ม "กู้คืน" ที่หน้า Admin (/admin) แทน — ดูรายละเอียดใน README.md หัวข้อ "สำรองและกู้คืนข้อมูล"
echo "สคริปต์นี้เลิกใช้แล้ว — กู้คืนข้อมูลได้จากหน้า Admin (/admin) แทน" >&2
exit 1
