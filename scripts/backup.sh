#!/usr/bin/env bash
# เลิกใช้แล้ว — ระบบสำรองข้อมูลย้ายเข้าไปอยู่ในแอปแล้ว
# ใช้ปุ่ม "สำรองข้อมูล" ที่หน้า Admin (/admin) แทน — ดูรายละเอียดใน README.md หัวข้อ "สำรองและกู้คืนข้อมูล"
# (ไฟล์นี้เก็บไว้เฉยๆ เพื่อไม่ให้ shell script เดิมพังหากมีใครเรียกใช้อยู่ — ไม่มีผลใดๆ)
echo "สคริปต์นี้เลิกใช้แล้ว — สำรองข้อมูลได้จากหน้า Admin (/admin) แทน" >&2
exit 1
